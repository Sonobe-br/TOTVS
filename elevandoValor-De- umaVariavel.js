/* 
* Elevando uma variável base ao quadrado
*/
let baseNumber = 2;
let exponent = 5;

const resultadoAoQuadrado = Math.pow(baseNumber, exponent);

const areaFunction = () => {

    let resultado = resultadoAoQuadrado;
    
    console.log(resultado);
    return resultado;

}
areaFunction();